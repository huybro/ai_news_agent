# --- Core Web App ---
fastapi
uvicorn[standard]
python-dotenv

# --- LangChain & LangGraph ---
# Installing the main packages will pull in the correct core libraries (e.g., langchain-core)
langchain
langgraph
langsmith

# --- LLM Provider ---
langchain-google-genai

# --- Checkpointers ---
# For PostgreSQL. This will install the correct 'psycopg' driver.
langgraph-checkpoint-postgres
# --- Tools ---
# You listed this, but your tool code uses the 'requests' library instead.
# Include if you are using it elsewhere.
newsapi-python
requests
